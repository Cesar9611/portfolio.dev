---
interface Props {
  id?: string;
  class?: string;
}

const { id, class: className } = Astro.props;
---

<section id={id} class=`w-full max-w-[810px] lg:w-[810px] mx-auto py-8 ${className}`>
  <slot />
</section>